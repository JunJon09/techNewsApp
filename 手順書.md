# 手順書：Step 1-1 〜 Step 1-4 の作業記録

> **ルール：新しい Step を実装したら必ずこのファイルに追記する。**

## 凡例
- 🤖 **自動生成** → コマンドが自動で作ったファイル（触らなくてOK）
- ✍️ **手書き** → Claude または自分が書いたファイル（中身を読むべき）
- 💻 **コマンド** → ターミナルで打ったコマンド

---

## Step 1-1：プロジェクト骨格の構築

### Bun のインストール

```bash
💻 curl -fsSL https://bun.sh/install | bash
💻 source ~/.zshrc   # 新しいターミナルを開いたときも必要
💻 bun --version     # 1.3.14 と出ればOK
```

---

### バックエンドの初期化

```bash
💻 mkdir backend
💻 cd backend
💻 bun init -y
```

**`bun init -y` が自動生成したファイル：**
```
backend/
├── 🤖 index.ts        → 削除した（src/index.ts に作り直した）
├── 🤖 tsconfig.json   → そのまま使用（strict: true が最初から入っている）
├── 🤖 package.json    → スクリプトを追加した
├── 🤖 .gitignore      → そのまま使用
├── 🤖 CLAUDE.md       → 削除した（ルートの CLAUDE.md を使うため）
└── 🤖 README.md       → 削除した
```

**不要ファイルを削除：**
```bash
💻 rm backend/CLAUDE.md backend/README.md backend/index.ts
```

---

### Hono と Vitest のインストール

```bash
💻 cd backend
💻 bun add hono
💻 bun add -d vitest @vitest/coverage-v8
```

`bun add` は `package.json` の `dependencies` に追記するだけ。

---

### ディレクトリ構造を作成

```bash
💻 mkdir -p backend/src/{routes,services,db/migrations,jobs,lib}
```

この時点では中身は空。

---

### 手書きしたファイル（バックエンド）

**✍️ `backend/src/index.ts`** （Hono アプリ本体）
```
Hono() でアプリを作り、GET /health を定義している。
AppType をエクスポートしているのは、将来 Hono RPC で
フロントエンドから型を使うための準備。
```

**✍️ `backend/src/server.ts`** （サーバー起動エントリポイント）
```
index.ts の app を受け取り、Bun のサーバーとして起動する。
スケジューラーもここで起動する（Step 1-3 で追加）。
```

**✍️ `backend/src/index.test.ts`** （ヘルスチェックのテスト）
```
app.request('/health') で実際にHonoアプリに
HTTPリクエストを送り、200が返るか確認している。
サーバーを起動しなくてもテストできるのがHonoの特徴。
```

**✍️ `backend/vitest.config.ts`** （Vitest の設定）
```
Vitest は Bun の .env を自動読み込みしないため、
DATABASE_URL などの環境変数をここで直接設定している。
```

**`backend/package.json` に追加したスクリプト：**
```json
"scripts": {
  "dev":           "bun run --hot src/server.ts",  ← ホットリロードで起動
  "test":          "bunx vitest run",               ← テスト実行
  "test:watch":    "bunx vitest",                   ← テスト監視モード
  "db:generate":   "drizzle-kit generate",          ← マイグレーション生成
  "db:migrate":    "bun run src/db/migrate.ts"      ← マイグレーション実行
}
```

---

### フロントエンドの初期化

```bash
💻 cd /techNewsApp
💻 bunx create-next-app@latest frontend --typescript --tailwind --app --no-src-dir --import-alias "@/*" --no-eslint --use-bun
```

**自動生成されたもの（中身はまだ触っていない）：**
```
frontend/
├── 🤖 app/           → Next.js のページ（Step 1-5 で書き直す）
├── 🤖 public/        → 静的ファイル
├── 🤖 tsconfig.json  → そのまま使用
├── 🤖 package.json   → そのまま使用
└── 🤖 ...その他設定ファイル
```

---

### Docker Compose の準備

**✍️ `docker-compose.yml`** （PostgreSQL + Redis を定義）
```
ラズパイ3を意識して mem_limit を設定している。
Redis は noeviction（BullMQ がジョブを消さないための設定）。
PostgreSQL のポートは 5433（Mac にローカルの PG が 5432 で動いているため）。
```

**✍️ `.env`** （環境変数）
```
DATABASE_URL / REDIS_URL / PORT を定義。
.gitignore に入れているのでGitには上がらない。
```

**✍️ `.gitignore`**

**コンテナを起動：**
```bash
💻 docker compose up -d

# 接続確認
💻 docker compose exec postgres psql -U technews -c "SELECT 1;"
💻 docker compose exec redis redis-cli ping   # PONG と返ればOK
```

---

## Step 1-2：Drizzle ORM セットアップ

### インストール

```bash
💻 cd backend
💻 bun add drizzle-orm postgres
💻 bun add -d drizzle-kit
```

- `drizzle-orm` → ORM 本体
- `postgres` → PostgreSQL ドライバー
- `drizzle-kit` → マイグレーション生成ツール（開発時のみ）

---

### 手書きしたファイル

**✍️ `backend/src/db/schema.ts`** （テーブル定義）
```
articles テーブルを TypeScript で定義している。
hn_id に UNIQUE 制約をつけているのは、同じ記事を二重登録しないため。
url が nullable なのは、HN には外部リンクのない記事（Ask HN など）があるため。
```

**✍️ `backend/src/db/client.ts`** （DB接続クライアント）
```
drizzle() で DB クライアントを作成してエクスポートする。
他のファイルはここから db をインポートして使う。
```

**✍️ `backend/drizzle.config.ts`** （Drizzle Kit の設定）
```
スキーマの場所とマイグレーション出力先を指定している。
```

**✍️ `backend/src/db/migrate.ts`** （マイグレーション実行スクリプト）
```
bun run db:migrate で呼ばれる。
migrations/ フォルダの SQL ファイルを PostgreSQL に適用する。
```

**✍️ `backend/src/db/client.test.ts`** （DB接続テスト）

---

### マイグレーションの実行

```bash
# SQL ファイルを自動生成（src/db/migrations/ に出力される）
💻 bun run db:generate

# 🤖 自動生成されたファイル
# src/db/migrations/0000_complete_sentry.sql

# Docker の PostgreSQL に SQL を直接適用
💻 docker compose exec -T postgres psql -U technews -d technews -f - < backend/src/db/migrations/0000_complete_sentry.sql

# テーブルが作られたか確認
💻 docker compose exec postgres psql -U technews -d technews -c "\d articles"
```

---

### テスト実行

```bash
💻 cd backend
💻 bunx vitest run
# 2 テスト通過（ヘルスチェック + DB接続）
```

---

## Step 1-3：Hacker News API 連携・BullMQ バッチ実装

### インストール

```bash
💻 cd backend
💻 bun add zod bullmq ioredis
💻 bun add @hono/zod-validator
```

- `zod` → 外部APIのレスポンスを型検証する
- `bullmq` → ジョブキュー（定期実行の仕組み）
- `ioredis` → Redis クライアント（BullMQ が内部で使う）
- `@hono/zod-validator` → Hono のリクエストバリデーション（Step 1-4 で使う）

---

### 手書きしたファイル

**✍️ `backend/src/lib/schemas/hn.schema.ts`** （Zod スキーマ）
```
Hacker News API のレスポンスを Zod で検証する。
外部APIのデータは型が保証されないため、必ずここを通す。
safeParse を使うことで、おかしいデータはエラーにせず null を返す。
```

**✍️ `backend/src/lib/hn-client.ts`** （HN API クライアント）
```
fetchTopStories() がメインの関数。流れは：
  1. トップ記事のIDリストを取得（最大 30件）
  2. Promise.all で全記事を並列取得（一件ずつ順番に待たない）
  3. 削除済み・dead・story以外の記事を除外して返す
```

**✍️ `backend/src/lib/redis.ts`** （Redis クライアント）
```
ioredis で Redis に接続する。
maxRetriesPerRequest: null は BullMQ が必要とする設定。
```

**✍️ `backend/src/jobs/fetch-news.job.ts`** （BullMQ ジョブ本体）
```
Queue → ジョブを入れる箱
Worker → 箱からジョブを取り出して実行する人

Worker の中でやっていること：
  1. fetchTopStories() で記事を取得
  2. DB に upsert（hn_id が重複していればスコアだけ更新）
  3. 完了をログに出力
```

**✍️ `backend/src/jobs/scheduler.ts`** （定期実行スケジューラー）
```
毎時0分に fetch-news ジョブをキューに追加する。
サーバー起動時に initial ジョブも即時追加するので、
起動直後にも一度ニュースを取得する。
```

**✍️ `backend/src/lib/hn-client.test.ts`** （HN クライアントのテスト）
```
実際の Hacker News API を叩かず、fetch をモックして検証している。
削除済み記事やコメントが除外されるかもテストしている。
```

---

### テスト実行・動作確認

```bash
💻 bunx vitest run
# 4 テスト通過

# サーバーを起動してニュース取得を確認
💻 bun run dev
# → [fetch-news] 29件取得
# → [fetch-news] DB保存完了

# DB にデータが入ったか確認
💻 docker compose exec postgres psql -U technews -d technews \
     -c "SELECT title, score FROM articles ORDER BY score DESC LIMIT 5;"
```

---

## 現在のファイル構成

```
techNewsApp/
├── ✍️ .env
├── ✍️ .gitignore
├── ✍️ CLAUDE.md
├── ✍️ docker-compose.yml
├── ✍️ plan.md
├── ✍️ 手順書.md（このファイル）
│
├── backend/
│   ├── ✍️ drizzle.config.ts
│   ├── ✍️ vitest.config.ts
│   ├── ✍️ .env
│   ├── 🤖 package.json（スクリプトは追記）
│   ├── 🤖 tsconfig.json
│   └── src/
│       ├── ✍️ index.ts        ← Hono アプリ・/health エンドポイント
│       ├── ✍️ server.ts       ← サーバー起動・スケジューラー呼び出し
│       ├── ✍️ index.test.ts
│       ├── db/
│       │   ├── ✍️ schema.ts   ← articles テーブル定義
│       │   ├── ✍️ client.ts   ← DB 接続クライアント
│       │   ├── ✍️ migrate.ts
│       │   ├── ✍️ client.test.ts
│       │   └── 🤖 migrations/0000_complete_sentry.sql
│       ├── jobs/
│       │   ├── ✍️ fetch-news.job.ts  ← BullMQ ジョブ本体
│       │   └── ✍️ scheduler.ts       ← Cron スケジューラー
│       └── lib/
│           ├── ✍️ hn-client.ts       ← HN API クライアント
│           ├── ✍️ hn-client.test.ts
│           ├── ✍️ redis.ts            ← Redis 接続
│           └── schemas/
│               └── ✍️ hn.schema.ts   ← Zod スキーマ
│
└── frontend/
    └── 🤖 （create-next-app が生成、Step 1-5 で編集予定）
```

---

## Step 1-4：バックエンド API 実装

### インストール

```bash
💻 cd backend
💻 bun add @opentelemetry/sdk-node @opentelemetry/auto-instrumentations-node
```

- `@opentelemetry/sdk-node` → OpenTelemetry の Node.js 向け SDK
- `@opentelemetry/auto-instrumentations-node` → HTTP・DB などを自動でトレースするプラグイン集

---

### 手書きしたファイル

**✍️ `backend/src/db/articles.repository.ts`** （DB アクセス層）
```
DB からスコア順で記事を取得するクエリだけを書く場所。
ページネーションのために limit と offset を受け取る。
offset = (page - 1) * limit で何件目から取るかを計算する。
```

**✍️ `backend/src/services/articles.service.ts`** （ビジネスロジック層）
```
Redis キャッシュを確認 → あれば即返す / なければ DB から取得してキャッシュ保存。
fromCache フラグを返すことで、キャッシュが効いているか確認できる。
キャッシュキーは "articles:${limit}:${page}" の形式。TTL は 5分（300秒）。
```

**✍️ `backend/src/routes/articles.route.ts`** （ルート層）
```
GET /api/articles?limit=30&page=1 のエンドポイントを定義。
zValidator で limit・page のクエリパラメータを検証している。
  - limit: 1〜100 の整数（デフォルト 30）
  - page: 1以上の整数（デフォルト 1）
ArticlesRouteType を export しているのは Hono RPC でフロントから型を使うため。
```

**✍️ `backend/src/lib/otel.ts`** （OpenTelemetry 初期化）
```
アプリ起動前に読み込むことでリクエストのトレースが有効になる。
fs のトレースは不要なので無効化している。
Step 1-6 で Grafana Tempo と繋いで可視化する予定。
```

**`backend/src/index.ts` を更新**
```
articlesRoute を /api 以下にマウントした。
Hono のメソッドチェーン（.route()）で追加することで
AppType に型が自動で含まれる。
```

**`backend/src/server.ts` を更新**
```
import './lib/otel' を一番上に追加。
OTel はアプリより先に初期化しないとトレースが取れないため。
```

**✍️ `backend/src/services/articles.service.test.ts`** （サービス層のテスト）
```
Redis と repository を vi.mock() でモック化してテスト。
  - キャッシュあり → DB を叩いていないことを確認
  - キャッシュなし → DB を叩いてキャッシュ保存することを確認
  - 2ページ目 → offset が正しく計算されているか確認
```

---

### テスト実行・動作確認

```bash
# 7テスト全通過を確認
💻 bunx vitest run

# サーバー起動
💻 bun run dev

# 記事一覧を取得（limit=3 で3件）
💻 curl -s "http://localhost:8080/api/articles?limit=3" | python3 -m json.tool
# → "fromCache": false（1回目はDBから）

# 同じリクエストをもう一度
💻 curl -s "http://localhost:8080/api/articles?limit=3" | python3 -m json.tool
# → "fromCache": true（2回目はRedisから）
```

---

### 現在のファイル構成（Step 1-4 完了時点）

```
backend/src/
├── ✍️ index.ts         ← articlesRoute を /api に追加
├── ✍️ server.ts        ← otel を先頭に import 追加
├── ✍️ index.test.ts
├── db/
│   ├── ✍️ schema.ts
│   ├── ✍️ client.ts
│   ├── ✍️ client.test.ts
│   ├── ✍️ migrate.ts
│   ├── ✍️ articles.repository.ts  ← NEW: DBクエリ
│   └── 🤖 migrations/
├── services/
│   ├── ✍️ articles.service.ts      ← NEW: キャッシュロジック
│   └── ✍️ articles.service.test.ts ← NEW: サービステスト
├── routes/
│   └── ✍️ articles.route.ts        ← NEW: エンドポイント定義
├── jobs/
│   ├── ✍️ fetch-news.job.ts
│   └── ✍️ scheduler.ts
└── lib/
    ├── ✍️ hn-client.ts
    ├── ✍️ hn-client.test.ts
    ├── ✍️ redis.ts
    ├── ✍️ otel.ts                  ← NEW: OpenTelemetry 初期化
    └── schemas/
        └── ✍️ hn.schema.ts
```
