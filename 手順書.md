# 手順書：Step 1-1 〜 Step 1-5 の作業記録

> **ルール：新しい Step を実装したら必ずこのファイルに追記する。**

## 凡例
- 🤖 **自動生成** → コマンドが自動で作ったファイル（触らなくてOK）
- ✍️ **手書き** → Claude または自分が書いたファイル（中身を読むべき）
- 💻 **コマンド** → ターミナルで打ったコマンド

---

## Step 1-1：プロジェクト骨格の構築

### Bun のインストール

```bash
💻 curl -fsSL https://bun.sh/install | bash
💻 source ~/.zshrc   # 新しいターミナルを開いたときも必要
💻 bun --version     # 1.3.14 と出ればOK
```

---

### バックエンドの初期化

```bash
💻 mkdir backend
💻 cd backend
💻 bun init -y
```

**`bun init -y` が自動生成したファイル：**
```
backend/
├── 🤖 index.ts        → 削除した（src/index.ts に作り直した）
├── 🤖 tsconfig.json   → そのまま使用（strict: true が最初から入っている）
├── 🤖 package.json    → スクリプトを追加した
├── 🤖 .gitignore      → そのまま使用
├── 🤖 CLAUDE.md       → 削除した（ルートの CLAUDE.md を使うため）
└── 🤖 README.md       → 削除した
```

**不要ファイルを削除：**
```bash
💻 rm backend/CLAUDE.md backend/README.md backend/index.ts
```

---

### Hono と Vitest のインストール

```bash
💻 cd backend
💻 bun add hono
💻 bun add -d vitest @vitest/coverage-v8
```

`bun add` は `package.json` の `dependencies` に追記するだけ。

---

### ディレクトリ構造を作成

```bash
💻 mkdir -p backend/src/{routes,services,db/migrations,jobs,lib}
```

この時点では中身は空。

---

### 手書きしたファイル（バックエンド）

**✍️ `backend/src/index.ts`** （Hono アプリ本体）
```
Hono() でアプリを作り、GET /health を定義している。
AppType をエクスポートしているのは、将来 Hono RPC で
フロントエンドから型を使うための準備。
```

**✍️ `backend/src/server.ts`** （サーバー起動エントリポイント）
```
index.ts の app を受け取り、Bun のサーバーとして起動する。
スケジューラーもここで起動する（Step 1-3 で追加）。
```

**✍️ `backend/src/index.test.ts`** （ヘルスチェックのテスト）
```
app.request('/health') で実際にHonoアプリに
HTTPリクエストを送り、200が返るか確認している。
サーバーを起動しなくてもテストできるのがHonoの特徴。
```

**✍️ `backend/vitest.config.ts`** （Vitest の設定）
```
Vitest は Bun の .env を自動読み込みしないため、
DATABASE_URL などの環境変数をここで直接設定している。
```

**`backend/package.json` に追加したスクリプト：**
```json
"scripts": {
  "dev":           "bun run --hot src/server.ts",  ← ホットリロードで起動
  "test":          "bunx vitest run",               ← テスト実行
  "test:watch":    "bunx vitest",                   ← テスト監視モード
  "db:generate":   "drizzle-kit generate",          ← マイグレーション生成
  "db:migrate":    "bun run src/db/migrate.ts"      ← マイグレーション実行
}
```

---

### フロントエンドの初期化

```bash
💻 cd /techNewsApp
💻 bunx create-next-app@latest frontend --typescript --tailwind --app --no-src-dir --import-alias "@/*" --no-eslint --use-bun
```

**自動生成されたもの（中身はまだ触っていない）：**
```
frontend/
├── 🤖 app/           → Next.js のページ（Step 1-5 で書き直す）
├── 🤖 public/        → 静的ファイル
├── 🤖 tsconfig.json  → そのまま使用
├── 🤖 package.json   → そのまま使用
└── 🤖 ...その他設定ファイル
```

---

### Docker Compose の準備

**✍️ `docker-compose.yml`** （PostgreSQL + Redis を定義）
```
ラズパイ3を意識して mem_limit を設定している。
Redis は noeviction（BullMQ がジョブを消さないための設定）。
PostgreSQL のポートは 5433（Mac にローカルの PG が 5432 で動いているため）。
```

**✍️ `.env`** （環境変数）
```
DATABASE_URL / REDIS_URL / PORT を定義。
.gitignore に入れているのでGitには上がらない。
```

**✍️ `.gitignore`**

**コンテナを起動：**
```bash
💻 docker compose up -d

# 接続確認
💻 docker compose exec postgres psql -U technews -c "SELECT 1;"
💻 docker compose exec redis redis-cli ping   # PONG と返ればOK
```

---

## Step 1-2：Drizzle ORM セットアップ

### インストール

```bash
💻 cd backend
💻 bun add drizzle-orm postgres
💻 bun add -d drizzle-kit
```

- `drizzle-orm` → ORM 本体
- `postgres` → PostgreSQL ドライバー
- `drizzle-kit` → マイグレーション生成ツール（開発時のみ）

---

### 手書きしたファイル

**✍️ `backend/src/db/schema.ts`** （テーブル定義）
```
articles テーブルを TypeScript で定義している。
hn_id に UNIQUE 制約をつけているのは、同じ記事を二重登録しないため。
url が nullable なのは、HN には外部リンクのない記事（Ask HN など）があるため。
```

**✍️ `backend/src/db/client.ts`** （DB接続クライアント）
```
drizzle() で DB クライアントを作成してエクスポートする。
他のファイルはここから db をインポートして使う。
```

**✍️ `backend/drizzle.config.ts`** （Drizzle Kit の設定）
```
スキーマの場所とマイグレーション出力先を指定している。
```

**✍️ `backend/src/db/migrate.ts`** （マイグレーション実行スクリプト）
```
bun run db:migrate で呼ばれる。
migrations/ フォルダの SQL ファイルを PostgreSQL に適用する。
```

**✍️ `backend/src/db/client.test.ts`** （DB接続テスト）

---

### マイグレーションの実行

```bash
# SQL ファイルを自動生成（src/db/migrations/ に出力される）
💻 bun run db:generate

# 🤖 自動生成されたファイル
# src/db/migrations/0000_complete_sentry.sql

# Docker の PostgreSQL に SQL を直接適用
💻 docker compose exec -T postgres psql -U technews -d technews -f - < backend/src/db/migrations/0000_complete_sentry.sql

# テーブルが作られたか確認
💻 docker compose exec postgres psql -U technews -d technews -c "\d articles"
```

---

### テスト実行

```bash
💻 cd backend
💻 bunx vitest run
# 2 テスト通過（ヘルスチェック + DB接続）
```

---

## Step 1-3：Hacker News API 連携・BullMQ バッチ実装

### インストール

```bash
💻 cd backend
💻 bun add zod bullmq ioredis
💻 bun add @hono/zod-validator
```

- `zod` → 外部APIのレスポンスを型検証する
- `bullmq` → ジョブキュー（定期実行の仕組み）
- `ioredis` → Redis クライアント（BullMQ が内部で使う）
- `@hono/zod-validator` → Hono のリクエストバリデーション（Step 1-4 で使う）

---

### 手書きしたファイル

**✍️ `backend/src/lib/schemas/hn.schema.ts`** （Zod スキーマ）
```
Hacker News API のレスポンスを Zod で検証する。
外部APIのデータは型が保証されないため、必ずここを通す。
safeParse を使うことで、おかしいデータはエラーにせず null を返す。
```

**✍️ `backend/src/lib/hn-client.ts`** （HN API クライアント）
```
fetchTopStories() がメインの関数。流れは：
  1. トップ記事のIDリストを取得（最大 30件）
  2. Promise.all で全記事を並列取得（一件ずつ順番に待たない）
  3. 削除済み・dead・story以外の記事を除外して返す
```

**✍️ `backend/src/lib/redis.ts`** （Redis クライアント）
```
ioredis で Redis に接続する。
maxRetriesPerRequest: null は BullMQ が必要とする設定。
```

**✍️ `backend/src/jobs/fetch-news.job.ts`** （BullMQ ジョブ本体）
```
Queue → ジョブを入れる箱
Worker → 箱からジョブを取り出して実行する人

Worker の中でやっていること：
  1. fetchTopStories() で記事を取得
  2. DB に upsert（hn_id が重複していればスコアだけ更新）
  3. 完了をログに出力
```

**✍️ `backend/src/jobs/scheduler.ts`** （定期実行スケジューラー）
```
毎時0分に fetch-news ジョブをキューに追加する。
サーバー起動時に initial ジョブも即時追加するので、
起動直後にも一度ニュースを取得する。
```

**✍️ `backend/src/lib/hn-client.test.ts`** （HN クライアントのテスト）
```
実際の Hacker News API を叩かず、fetch をモックして検証している。
削除済み記事やコメントが除外されるかもテストしている。
```

---

### テスト実行・動作確認

```bash
💻 bunx vitest run
# 4 テスト通過

# サーバーを起動してニュース取得を確認
💻 bun run dev
# → [fetch-news] 29件取得
# → [fetch-news] DB保存完了

# DB にデータが入ったか確認
💻 docker compose exec postgres psql -U technews -d technews \
     -c "SELECT title, score FROM articles ORDER BY score DESC LIMIT 5;"
```

---

## 現在のファイル構成

```
techNewsApp/
├── ✍️ .env
├── ✍️ .gitignore
├── ✍️ CLAUDE.md
├── ✍️ docker-compose.yml
├── ✍️ plan.md
├── ✍️ 手順書.md（このファイル）
│
├── backend/
│   ├── ✍️ drizzle.config.ts
│   ├── ✍️ vitest.config.ts
│   ├── ✍️ .env
│   ├── 🤖 package.json（スクリプトは追記）
│   ├── 🤖 tsconfig.json
│   └── src/
│       ├── ✍️ index.ts        ← Hono アプリ・/health エンドポイント
│       ├── ✍️ server.ts       ← サーバー起動・スケジューラー呼び出し
│       ├── ✍️ index.test.ts
│       ├── db/
│       │   ├── ✍️ schema.ts   ← articles テーブル定義
│       │   ├── ✍️ client.ts   ← DB 接続クライアント
│       │   ├── ✍️ migrate.ts
│       │   ├── ✍️ client.test.ts
│       │   └── 🤖 migrations/0000_complete_sentry.sql
│       ├── jobs/
│       │   ├── ✍️ fetch-news.job.ts  ← BullMQ ジョブ本体
│       │   └── ✍️ scheduler.ts       ← Cron スケジューラー
│       └── lib/
│           ├── ✍️ hn-client.ts       ← HN API クライアント
│           ├── ✍️ hn-client.test.ts
│           ├── ✍️ redis.ts            ← Redis 接続
│           └── schemas/
│               └── ✍️ hn.schema.ts   ← Zod スキーマ
│
└── frontend/
    └── 🤖 （create-next-app が生成、Step 1-5 で編集予定）
```

---

## Step 1-4：バックエンド API 実装

### インストール

```bash
💻 cd backend
💻 bun add @opentelemetry/sdk-node @opentelemetry/auto-instrumentations-node
```

- `@opentelemetry/sdk-node` → OpenTelemetry の Node.js 向け SDK
- `@opentelemetry/auto-instrumentations-node` → HTTP・DB などを自動でトレースするプラグイン集

---

### 手書きしたファイル

**✍️ `backend/src/db/articles.repository.ts`** （DB アクセス層）
```
DB からスコア順で記事を取得するクエリだけを書く場所。
ページネーションのために limit と offset を受け取る。
offset = (page - 1) * limit で何件目から取るかを計算する。
```

**✍️ `backend/src/services/articles.service.ts`** （ビジネスロジック層）
```
Redis キャッシュを確認 → あれば即返す / なければ DB から取得してキャッシュ保存。
fromCache フラグを返すことで、キャッシュが効いているか確認できる。
キャッシュキーは "articles:${limit}:${page}" の形式。TTL は 5分（300秒）。
```

**✍️ `backend/src/routes/articles.route.ts`** （ルート層）
```
GET /api/articles?limit=30&page=1 のエンドポイントを定義。
zValidator で limit・page のクエリパラメータを検証している。
  - limit: 1〜100 の整数（デフォルト 30）
  - page: 1以上の整数（デフォルト 1）
ArticlesRouteType を export しているのは Hono RPC でフロントから型を使うため。
```

**✍️ `backend/src/lib/otel.ts`** （OpenTelemetry 初期化）
```
アプリ起動前に読み込むことでリクエストのトレースが有効になる。
fs のトレースは不要なので無効化している。
Step 1-6 で Grafana Tempo と繋いで可視化する予定。
```

**`backend/src/index.ts` を更新**
```
articlesRoute を /api 以下にマウントした。
Hono のメソッドチェーン（.route()）で追加することで
AppType に型が自動で含まれる。
```

**`backend/src/server.ts` を更新**
```
import './lib/otel' を一番上に追加。
OTel はアプリより先に初期化しないとトレースが取れないため。
```

**✍️ `backend/src/services/articles.service.test.ts`** （サービス層のテスト）
```
Redis と repository を vi.mock() でモック化してテスト。
  - キャッシュあり → DB を叩いていないことを確認
  - キャッシュなし → DB を叩いてキャッシュ保存することを確認
  - 2ページ目 → offset が正しく計算されているか確認
```

---

### テスト実行・動作確認

```bash
# 7テスト全通過を確認
💻 bunx vitest run

# サーバー起動
💻 bun run dev

# 記事一覧を取得（limit=3 で3件）
💻 curl -s "http://localhost:8080/api/articles?limit=3" | python3 -m json.tool
# → "fromCache": false（1回目はDBから）

# 同じリクエストをもう一度
💻 curl -s "http://localhost:8080/api/articles?limit=3" | python3 -m json.tool
# → "fromCache": true（2回目はRedisから）
```

---

### 現在のファイル構成（Step 1-4 完了時点）

```
backend/src/
├── ✍️ index.ts         ← articlesRoute を /api に追加
├── ✍️ server.ts        ← otel を先頭に import 追加
├── ✍️ index.test.ts
├── db/
│   ├── ✍️ schema.ts
│   ├── ✍️ client.ts
│   ├── ✍️ client.test.ts
│   ├── ✍️ migrate.ts
│   ├── ✍️ articles.repository.ts  ← NEW: DBクエリ
│   └── 🤖 migrations/
├── services/
│   ├── ✍️ articles.service.ts      ← NEW: キャッシュロジック
│   └── ✍️ articles.service.test.ts ← NEW: サービステスト
├── routes/
│   └── ✍️ articles.route.ts        ← NEW: エンドポイント定義
├── jobs/
│   ├── ✍️ fetch-news.job.ts
│   └── ✍️ scheduler.ts
└── lib/
    ├── ✍️ hn-client.ts
    ├── ✍️ hn-client.test.ts
    ├── ✍️ redis.ts
    ├── ✍️ otel.ts                  ← NEW: OpenTelemetry 初期化
    └── schemas/
        └── ✍️ hn.schema.ts
```

---

## Step 1-4 追加変更：JST 統一・repository リネーム

### 背景
毎日0時に取得した記事を「その日のランキング」として管理するため、バックエンド全体の時刻を JST に統一した。

### 変更したファイル

**`backend/.env` に追加**
```
TZ=Asia/Tokyo
```
Bun・Cron が JST で動くようになる。Cron `'0 0 * * *'` が自動で JST 0時として動く。

**`docker-compose.yml` の postgres に追加**
```yaml
PGTZ: Asia/Tokyo
```
PostgreSQL の `now()` と `CURRENT_DATE` が JST になる。

**`backend/vitest.config.ts` に追加**
```
TZ: 'Asia/Tokyo'
```
テスト環境も JST に揃える。

**`backend/src/db/articles.repository.ts` を更新**
```
findArticles → findTodayArticles にリネーム
  WHERE DATE(fetched_at) = CURRENT_DATE フィルターを追加（今日の記事だけ返す）

findArticlesByDate(date: string) を新規追加
  YYYYMMDD 形式の文字列を受け取り、その日の記事を全件返す
  ページネーションなし（1日最大30件のため）
```

**`backend/src/services/articles.service.ts` を更新**
```
findArticles → findTodayArticles に import 名を変更
```

**`backend/src/services/articles.service.test.ts` を更新**
```
モックの関数名を findArticles → findTodayArticles に変更
```

### テスト実行
```bash
💻 bunx vitest run
# 7テスト全通過
```

---

## Step 1-4 追加変更：findArticlesByDate ページネーション対応

### 背景
日付指定の記事取得も今日の記事と同様にページネーション対応することにした。

### 変更したファイル

**`backend/src/db/articles.repository.ts`**
```
findArticlesByDate(date: string)
  → findArticlesByDate(date: string, limit: number, offset: number) に変更
```

**`backend/src/services/articles.service.ts`**
```
getArticlesByDate(date, limit, page) を新規追加
  - キャッシュキーは "articles:20260616:10:1" の形式
  - getArticles と同じキャッシュ・DB取得ロジック
buildCacheKey に prefix を追加し、今日用・日付指定用で区別できるようにした
  - 今日: "articles:today:30:1"
  - 日付指定: "articles:20260616:10:1"
```

**`backend/src/routes/articles.route.ts`**
```
GET /api/articles/:date?limit=10&page=1 を新規追加
  - :date は YYYYMMDD 形式（正規表現 /^\d{8}$/ でバリデーション）
  - zValidator で param と query 両方を検証
```

**`backend/src/services/articles.service.test.ts`**
```
getArticlesByDate のテストを2件追加
  - キャッシュヒット時
  - キャッシュミス時（DB取得・キャッシュ保存）
合計 9テスト
```

### テスト実行
```bash
💻 bunx vitest run
# 9テスト全通過
```

### 動作確認（curl）
```bash
# 今日の記事
💻 curl -s "http://localhost:8080/api/articles?limit=3"

# 日付指定（YYYYMMDD）
💻 curl -s "http://localhost:8080/api/articles/20260616?limit=3&page=1"
```

---

## Step 1-5：フロントエンド実装

### 追加変更（バックエンド）

TypeScript エラー修正: `bullmq` が内部に独自の `ioredis` をバンドルしているため、
外部の `ioredis` インスタンスを渡すと型が競合する。
BullMQ には接続オプション（`host/port`）を渡すよう変更。

✍️ `backend/src/lib/redis.ts`
```typescript
// 追加: BullMQ 専用の接続設定（インスタンスではなくオプション値）
const parsed = new URL(redisUrl)
export const bullmqConnection = {
  host: parsed.hostname,
  port: parseInt(parsed.port || '6379'),
} as const
```

✍️ `backend/src/jobs/fetch-news.job.ts`
```typescript
// redisConnection → bullmqConnection に変更
import { bullmqConnection } from '../lib/redis'
```

---

### 依存パッケージのインストール

```bash
💻 cd frontend
💻 bun add hono              # Hono RPC クライアント（hc 関数）
💻 bun add -d @playwright/test  # E2E テスト
```

---

### フロントエンドの設定ファイル

✍️ `frontend/tsconfig.json`（変更）
```json
// include に追加 → バックエンドの型をフロントから参照できるようにする
"../backend/src/**/*.ts"
```
> `import type { AppType }` は `import type` なのでバンドルには含まれない。TypeScript の型チェックにのみ使われる。

✍️ `frontend/.env.local`（新規作成）
```
BACKEND_URL=http://localhost:8080
```
> `BACKEND_URL` はサーバーサイド（Server Components）からバックエンドへアクセスするURL。

---

### Hono RPC クライアント

✍️ `frontend/lib/api.ts`（新規作成）
```typescript
import { hc } from 'hono/client'
import type { AppType } from '../../backend/src/index'

export const apiClient = hc<AppType>(process.env.BACKEND_URL ?? 'http://localhost:8080')
```
> `hc<AppType>(url)` でバックエンドのルート型を継承したクライアントが生成される。
> `apiClient.api.articles.$get(...)` のように型補完が効く。

---

### UI コンポーネント

✍️ `frontend/app/layout.tsx`（変更）
- `lang="ja"` に変更
- タイトルを `HN Tech News`、description を日本語に変更
- body に `bg-gray-50 dark:bg-gray-950` を追加

✍️ `frontend/app/page.tsx`（全面置き換え）
- `async` な Server Component として実装
- `apiClient.api.articles.$get()` で記事を取得
- 記事カード：ランク番号・タイトル（外部リンク）・ドメイン・スコア（▲）・著者・コメント数
- `data-testid="article-card"` を付与（E2E テスト用）
- 記事0件のときは「今日の記事はまだありません」を表示

✍️ `frontend/app/loading.tsx`（新規作成）
- `animate-pulse` のスケルトン UI
- ページ読み込み中に Suspense が自動的にこれを表示する

✍️ `frontend/app/error.tsx`（新規作成）
- Client Component（`'use client'`）
- Next.js 16 の新仕様: `reset` の代わりに `unstable_retry` を使用
- バックエンド接続失敗時のフォールバック表示

---

### E2E テスト（Playwright）

✍️ `frontend/playwright.config.ts`（新規作成）
- ベース URL: `http://localhost:3000`
- `webServer` で `bun run dev` を自動起動（既存サーバーがあれば再利用）

✍️ `frontend/e2e/home.spec.ts`（新規作成）
```
テスト1: タイトル「HN Tech News」が表示される
テスト2: 記事カードまたは空メッセージが表示される
テスト3: 記事がある場合はスコア（▲）と著者名が表示される
```

`frontend/package.json` に追加:
```json
"test:e2e":    "bunx playwright test",
"test:e2e:ui": "bunx playwright test --ui"
```

---

### 動作確認

```bash
# バックエンドを起動
💻 cd backend && bun run dev

# フロントエンドを起動（別ターミナル）
💻 cd frontend && bun run dev

# ブラウザで確認
💻 open http://localhost:3000
# → HN Tech News タイトル、記事カード一覧が表示される

# TypeScript エラーがないことを確認
💻 bunx tsc --noEmit
# → エラーなし

# バックエンドテスト（念のため）
💻 bunx vitest run
# → 9テスト全通過
```

---

## Step 1-6：Docker Compose 統合・ローカル動作確認

### バックエンドに Prometheus メトリクスを追加

```bash
💻 bun add prom-client
```

✍️ `backend/src/lib/metrics.ts`（新規作成）
```typescript
// prom-client で Registry を作成し、デフォルトメトリクス（CPU・メモリ等）と
// HTTPリクエスト数カウンター・レスポンスタイムヒストグラムを登録
```

✍️ `backend/src/index.ts`（変更）
- `'*'` に Prometheus 収集ミドルウェアを追加（method・path・status ラベル付き）
- `GET /metrics` エンドポイントを追加（`prom-client` の `register.metrics()` を返す）

---

### フロントエンドの変更

✍️ `frontend/next.config.ts`（変更）
```typescript
output: 'standalone'  // Dockerイメージを小さくするためのスタンドアロン出力
```

✍️ `frontend/app/page.tsx`（変更）
```typescript
// ビルド時に静的プリレンダリングしないよう指定
// （バックエンドが起動していない状態で next build が実行されるため）
export const dynamic = 'force-dynamic'
```

---

### Dockerfile

✍️ `backend/Dockerfile`（新規作成）
```dockerfile
FROM oven/bun:1-alpine AS deps  # 依存関係インストール
FROM oven/bun:1-alpine AS runner  # 実行イメージ（src をコピーして Bun で起動）
```

✍️ `frontend/Dockerfile`（新規作成）
```
コンテキスト: モノレポルート（tsconfig が ../backend/src を参照するため）
- deps: bun install
- builder: backend/node_modules もインストール（型チェック用）→ next build
- runner: node:20-alpine + Next.js スタンドアロン出力
```

> **ハマりポイント①**: `bun.lockb`（バイナリ）ではなく `bun.lock`（テキスト）が正しいファイル名
> **ハマりポイント②**: ビルド時に `/api/articles` を叩こうとして失敗 → `force-dynamic` で解決
> **ハマりポイント③**: `drizzle-orm` が解決できない → builder で `backend/node_modules` もインストール

---

### Nginx 設定

✍️ `nginx/nginx.conf`（新規作成）
```
/api/*   → http://backend:8080
/health  → http://backend:8080
/metrics → http://backend:8080 (Docker内部ネットワークのみ)
/        → http://frontend:3000
```

---

### Prometheus / Grafana

✍️ `monitoring/prometheus.yml`
```yaml
# hono-backend を 15秒ごとにスクレイプ、保存期間7日
```

✍️ `monitoring/grafana/provisioning/datasources/prometheus.yml`
```yaml
# Prometheus データソースを自動登録（起動時に自動設定）
```

✍️ `monitoring/grafana/provisioning/dashboards/dashboard.yml`
```yaml
# /etc/grafana/dashboards をダッシュボード配置先として登録
```

✍️ `monitoring/grafana/dashboards/backend.json`
```
パネル:
- HTTPリクエスト数（/分）
- レスポンスタイム p50/p95
- プロセスメモリ使用量（MB）
- プロセス稼働時間（時間）
```

---

### docker-compose.yml 完成

✍️ `docker-compose.yml`（全面更新）
追加サービス: backend / frontend / nginx / prometheus / grafana
```yaml
全コンテナに mem_limit と restart: always を設定
メモリ配分:
  postgres: 256m / redis: 160m / backend: 256m
  frontend: 384m / nginx: 64m
  prometheus: 256m / grafana: 256m
```

---

### 動作確認

```bash
💻 docker compose build
# → backend・frontend ともに成功

💻 docker compose up -d
# → 7コンテナ全起動

# Nginx 経由でフロントエンドとAPI
💻 curl http://localhost:80       # → HN Tech News HTML
💻 curl "http://localhost:80/api/articles?limit=2"  # → JSON

# Prometheus ターゲット確認
💻 curl http://localhost:9090/api/v1/targets
# → hono-backend が "up"

# ブラウザで確認
open http://localhost:80    # ニュース一覧
open http://localhost:9090  # Prometheus
open http://localhost:3001  # Grafana（admin/admin）
```

---

## Step 1-7：GitHub Actions CI/CD 構築

### ワークフローの設計思想

| 段階 | どこで動く | 何をする |
|------|-----------|---------|
| CI | GitHub クラウド | テスト・型チェック（PR 時） |
| CD ビルド | GitHub クラウド | Docker イメージ作成・GHCR push（main マージ時） |
| CD デプロイ | **手動**（ラズパイ上で実行） | pull & 再起動 |

> CDのデプロイ自動化（SSH経由）はラズパイのネットワーク設定が決まってから追加する。
> 現時点ではイメージを GHCR に push するところまで自動化し、ラズパイへの適用は手動コマンドで行う。

---

### CI ワークフロー

✍️ `.github/workflows/ci.yml`（新規作成）

**トリガー**: `main` へのpush・PR

**job1: test-backend**
```
services:
  postgres:16-alpine (port 5433)  ← client.test.ts が実DB を使うため必要
  redis:7-alpine (port 6379)

手順:
  1. bun install
  2. bun run src/db/migrate.ts  ← テーブルを作ってからテスト実行
  3. bunx vitest run
```

**job2: lint-frontend**
```
手順:
  1. bun install (frontend)
  2. cd ../backend && bun install  ← tsconfig が ../backend/src を参照するため必要
  3. bunx tsc --noEmit
```

---

### CD ワークフロー

✍️ `.github/workflows/deploy.yml`（新規作成）

**トリガー**: `main` へのpush

**ビルド対象プラットフォーム**
```
linux/amd64   ← Mac / GitHub Actions ランナー
linux/arm64   ← ラズパイ3（64bit OS）
```

**push 先イメージ名**
```
ghcr.io/junjon09/technewsapp-backend:latest
ghcr.io/junjon09/technewsapp-backend:<commit-sha>
ghcr.io/junjon09/technewsapp-frontend:latest
ghcr.io/junjon09/technewsapp-frontend:<commit-sha>
```

> Buildx のキャッシュ（`type=gha`）を使うことで2回目以降のビルドを大幅に高速化。

---

### ラズパイ本番用オーバーライド

✍️ `docker-compose.prod.yml`（新規作成）
```yaml
# build: を無効化し、GHCR のイメージを使う
backend:
  image: ghcr.io/junjon09/technewsapp-backend:latest
frontend:
  image: ghcr.io/junjon09/technewsapp-frontend:latest
```

---

### DBマイグレーションの手順

`drizzle-kit` は devDependency のため本番コンテナに入っていない。
そのため `db:generate`（SQLファイル生成）と `db:migrate`（DB適用）を分けて実行する。

**① SQLファイルの生成（Mac上で実行）**

`bun` がMacに入っていない場合、一時コンテナを使う：

```bash
# 🤖 backend/ ディレクトリで実行
cd backend
docker run --rm \
  -v $(pwd):/app \
  -w /app \
  oven/bun:1-alpine \
  sh -c "bun install && bun run db:generate"
```

生成されたファイルは `backend/src/db/migrations/` に出力される。
コミット＆プッシュしてラズパイに反映する。

**② DBへの適用（ラズパイ上で実行）**

```bash
# 🤖 ラズパイ上で実行
docker compose exec backend bun run src/db/migrate.ts
```

---

### ラズパイへのデプロイ手順（手動）

```bash
# ① 初回のみ：GHCR にログイン
💻 docker login ghcr.io -u JunJon09 -p <GitHub Personal Access Token>

# ② main にマージ後、ラズパイ上で実行
💻 docker compose -f docker-compose.yml -f docker-compose.prod.yml pull
💻 docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

> GitHub Personal Access Token は `repo` + `read:packages` スコープが必要。
> GitHub → Settings → Developer settings → Personal access tokens で発行。
